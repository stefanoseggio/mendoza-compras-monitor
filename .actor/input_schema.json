{
    "$schema": "https://apify.com/schemas/v1/input.ide.json",
    "title": "Mendoza Compras Monitor Configuration",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "maxItems": {
            "title": "Max processes to extract",
            "type": "integer",
            "description": "Hard cap on the number of RAW processes walked this run (10 per page, 25,000+ total at audit time). This is the same cap as before - onlyNew/dateRange are applied on top of this raw walk as post-filters, so the number of records actually returned can be lower than maxItems when either is active. This actor does not yet expose the advanced-search form's own filters (date range, organism, process type) as actor input; start small, raise gradually.",
            "default": 100,
            "minimum": 1
        },
        "onlyNew": {
            "title": "Only new records (delta mode)",
            "type": "boolean",
            "description": "Persists which numeroProceso ids this actor has already returned, in its own private key-value store that survives between scheduled runs, and keeps only the ones not seen before. IMPORTANT for this source: comprar.mendoza.gov.ar's process listing is sorted by numero de proceso ascending (each organism's own sequential counter, cycling through years) - NOT by date and NOT newest-first. Verified live: a blank-form search's first page mixes processes from 2019 through 2026 rather than showing only the most recent ones. Because of this, onlyNew is a safe POST-FILTER (fetch up to maxItems raw processes exactly as a normal run does, then drop the ones already seen) rather than an early-stop optimization - it does NOT make pagination stop sooner, and it does NOT guarantee a newly created process will appear within a small maxItems window, since its position in the list depends on its own organism's counter, not on when it was published. Raise maxItems to cover more of the backlog if a delta run keeps returning fewer new records than expected. See AGENTS.md for the full live evidence.",
            "default": false
        },
        "dateRange": {
            "title": "Filter by opening date",
            "type": "string",
            "description": "Restricts the (raw, up-to-maxItems) results to processes whose own 'Fecha de apertura' (scheduled bid-opening date/time) falls within this window ending now. Independent of onlyNew. Note: Fecha de apertura is WHEN BIDS OPEN, not when the process was published on the portal - a currently-open tender is routinely dated in the future relative to when it first appeared, so this answers 'opens/opened in the last N', not 'listed in the last N'. A future-dated process never matches any window until its own opening date falls inside it. See README for detail.",
            "editor": "select",
            "enum": ["24h", "7d", "30d"],
            "enumTitles": ["Last 24 hours", "Last 7 days", "Last 30 days"]
        }
    }
}
